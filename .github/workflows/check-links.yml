name: check-links
on:
  workflow_dispatch:
  schedule:
    - cron: '37 8 * * *'
  push:

jobs:
  check-links:
    runs-on: ubuntu-latest
    steps:
      - uses: filiph/linkcheck@2.0.23
        with:
          arguments: -e https://yftdtddh.github.io/muxizyhz/
